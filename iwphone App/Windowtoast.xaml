<Window x:Class="iwphone.Windowtoast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iwphone"
        mc:Ignorable="d"
        Width="300" Height="80" Background="#333" Foreground="White"
        Margin="10" Padding="15"
        Visibility="Collapsed">
    <Grid>
        <!-- 图标区域 -->
        <Image Source="{Binding Icon}" Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <!-- 内容区域 -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="45,0,0,0">
            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
            <TextBlock Text="{Binding Message}" FontSize="12" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- 关闭按钮 -->
        <Button Content="×" HorizontalAlignment="Right" VerticalAlignment="Top"
                Background="Transparent" BorderBrush="Transparent"
                Foreground="White" Click="CloseButton_Click"/>
    </Grid>

    <!-- 动画定义 -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!-- 淡入动画 -->
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                     From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
